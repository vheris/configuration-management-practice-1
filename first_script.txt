# Этот скрипт демонстрирует все возможности

# Команда ls без аргументов
ls

# Команда ls с аргументами  
ls -la
ls -l -a -h

# Команда cd с разными аргументами
cd /home/user
cd ..
cd /usr/local/bin

# Работа с переменными окружения
$HOME
$USER
$PATH

# Пустая строка (игнорируется)

# Неизвестная переменная (вызовет ошибку)
$UNKNOWN_VAR

# Неизвестная команда (вызовет ошибку)
unknown_command
